# kotlin_spring_batch_test

```shell
./gradlew clean build
docker build -t batch:0.2 ./
docker tag batch:0.2 sejoung/batch:0.2
docker push sejoung/batch:0.2
docker rmi batch:0.2 sejoung/batch:0.2

```
